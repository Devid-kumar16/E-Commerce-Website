// src/admin/components/Orders.jsx
import React from "react";

export default function Orders(){
  // simple demo - show static message
  return (
    <div>
      <h2>Orders</h2>
      <div className="card">
        <div className="small">Orders are saved per-customer in the storefront demo. Use the storefront to place orders, then view them under user profile.</div>
      </div>
    </div>
  );
}
